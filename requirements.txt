# Core data
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
PyYAML==6.0.3
joblib==1.5.3

# Modelagem
scikit-learn==1.9.0
lightgbm==4.6.0
shap==0.52.0

# Análise / notebooks
matplotlib==3.11.0
jupyter==1.1.1
notebook==7.6.0

# Serviço de predição
fastapi==0.138.1
uvicorn==0.49.0
streamlit==1.58.0
requests==2.34.2   # o dashboard consome a API por HTTP
pydantic==2.13.4
duckdb==1.5.5

# Observabilidade — /metrics no formato Prometheus.
# prometheus-client já vinha transitivo do jupyter; pinar torna explícito e
# impede que a API quebre se o Jupyter sair do requirements.
prometheus-client==0.26.0
prometheus-fastapi-instrumentator==8.1.0

# Testes
pytest==9.1.1
httpx==0.28.1
